# RenWork AI 外贸增长系统｜界面设计规范

## 设计定位

- **类型**：专业 SaaS + 战术型运营仪表盘。
- **用户**：外贸老板、销售主管、业务员、市场运营、跟单与品质人员。
- **首要任务**：快速判断系统是否可信可用，并从“事实证据 → 客户优先级 → 下一动作”推进工作。
- **品牌形容词**：可信、克制、锋利、可审计、行动导向。
- **三词审美**：航运控制台 / 证据档案 / 商务执行。

## 视觉承诺

- **方向**：深海蓝灰的出口控制台，信号橙只用于主操作与关键告警；不使用 AI 产品常见紫色渐变。
- **字体**：标题 `IBM Plex Sans Condensed`，正文 `Source Han Sans SC`；离线环境回退到可读本地字体。
- **字号**：基准 16px，比例 1.25；12.8 / 16 / 20 / 25 / 31.25 / 39px。
- **间距**：4px 基础单位；组内 8–12px，模块间 24–40px。
- **圆角**：4px 控件，8px 大面板；禁止泡泡式大圆角。
- **边界**：采用清晰描边，不叠加宽泛阴影。
- **签名动作**：**证据航线 Evidence Rail**。顶部横向状态轨道持续显示身份、租户、存储、连接器和公开闸门状态，让“能否相信”始终先于“生成什么”。

## 语义令牌

| 角色 | OKLCH |
|---|---|
| 背景 | `oklch(0.975 0.008 230)` |
| 主表面 | `oklch(0.995 0.003 230)` |
| 深色导航 | `oklch(0.225 0.035 242)` |
| 主文字 | `oklch(0.245 0.025 242)` |
| 次文字 | `oklch(0.48 0.025 242)` |
| 描边 | `oklch(0.84 0.018 230)` |
| 主操作 | `oklch(0.64 0.17 45)` |
| 成功 | `oklch(0.54 0.12 155)` |
| 警告 | `oklch(0.7 0.15 75)` |
| 错误 | `oklch(0.56 0.2 28)` |

## 布局与组件

- 12 列桌面网格；状态轨道全宽，知识模块 8 列，操作/连接 4 列。
- 关键状态在左上；不把所有信息做成相同卡片。
- 数值右对齐并使用等宽数字；列表用轻分隔线。
- 输入始终有可见标签；API Key 使用密码框且只存 `sessionStorage`。
- 提供未连接、加载、成功、错误和能力不可用状态。
- 所有按钮支持 hover、active、focus、disabled；焦点环对比度满足 WCAG 2.2 AA。
- 交互目标至少 44px；响应式在 760px 以下改为单列。

## 动效

- 只对状态徽标和请求反馈使用 160–220ms 的 opacity/transform；不做连续装饰动画。
- `prefers-reduced-motion` 下取消位移动效。

## 无障碍

- 原生表单、按钮和列表；状态更新使用 `aria-live=polite`。
- 状态不仅靠颜色，同时有文字（就绪、未配置、不可用）。
- 键盘顺序与视觉顺序一致；无鼠标也可完成连接、冷启动与搜索。
- 正文对比度按 AA，最小字号 14px。

## 自审

- 无紫色渐变、玻璃拟态、渐变文字、图标卡片墙、股票人物图或无意义动效。
- 信息密度服务于“判断可信度和下一动作”，不是原始数据堆砌。
- 签名动作“证据航线”清晰可辨。
- Slop checklist：10/10；WCAG 2.2 AA 设计门槛：通过静态审查，仍需真实浏览器键盘与读屏回归。

## 变更记录

- 2026-08-24：V4 初版，重构为证据优先的战术运营控制台。
